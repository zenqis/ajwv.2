export const AJW_NEON_TABLES = [
  { name: "ajw_employees", pk: "id", sql: "create table if not exists ajw_employees (id text primary key, data jsonb not null, nama text, jabatan text, updated_at timestamptz default now())" },
  { name: "ajw_eval", pk: "id", sql: "create table if not exists ajw_eval (id text primary key, data jsonb not null, nama text, grade text, nilai numeric default 0, updated_at timestamptz default now())" },
  { name: "ajw_payroll", pk: "id", sql: "create table if not exists ajw_payroll (id text primary key, data jsonb not null, nama text, gaji_bersih numeric default 0, updated_at timestamptz default now())" },
  { name: "ajw_kpi", pk: "periode", sql: "create table if not exists ajw_kpi (id bigserial, periode text primary key, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_supplier", pk: "id", sql: "create table if not exists ajw_supplier (id text primary key, data jsonb not null, nama_supplier text, bulan text, tahun int, updated_at timestamptz default now())" },
  { name: "ajw_config", pk: "key", sql: "create table if not exists ajw_config (key text primary key, value jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_hr_sops", pk: "id", sql: "create table if not exists ajw_hr_sops (id text primary key, title text, department text, stage text, doc_type text, file_name text, file_type text, file_size bigint default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_hr_control", pk: "id", sql: "create table if not exists ajw_hr_control (id text primary key, status text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_dev_hub", pk: "id", sql: "create table if not exists ajw_dev_hub (id text primary key, section_count int default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_customer_data", pk: "id", sql: `create table if not exists ajw_customer_data (
    id text primary key, upload_session_id text, upload_session_label text, source_type text, tanggal date,
    period_from date, period_to date, order_number text, package_type text, marketplace text, store_marketplace text,
    store_name text, buyer_name text, receiver_name text, phone text, postal_code text, province text, city text,
    district text, address text, sku text, warehouse_sku text, warehouse_sku_name text, product_name text,
    variant_name text, quantity numeric default 0, unit_price numeric default 0, product_subtotal numeric default 0,
    product_cost numeric default 0, shipping_service text, tracking_number text, total_order numeric default 0,
    payment_method text, order_created_at timestamptz, completed_at timestamptz, country text, data jsonb not null,
    updated_at timestamptz default now())` },
  { name: "ajw_analytics_data", pk: "id", sql: "create table if not exists ajw_analytics_data (id text primary key, scope text, rows_count int default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_refunds", pk: "id", sql: "create table if not exists ajw_tool_refunds (id text primary key, input_date date, order_no text, marketplace text, item_name text, nominal numeric default 0, group_id text, image_name text, image_data text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_complaints", pk: "id", sql: "create table if not exists ajw_tool_complaints (id text primary key, input_date date, order_no text, marketplace text, issue_type text, nominal numeric default 0, group_id text, image_name text, image_data text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_requests", pk: "id", sql: "create table if not exists ajw_tool_requests (id text primary key, input_date date, title text, division text, priority text, status text, group_id text, image_name text, image_data text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_material_stock", pk: "id", sql: "create table if not exists ajw_tool_material_stock (id text primary key, nama text, kategori text, satuan text, stok_akhir numeric default 0, status text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_material_orders", pk: "id", sql: "create table if not exists ajw_tool_material_orders (id text primary key, session_id text, material_id text, nama text, qty numeric default 0, subtotal numeric default 0, ordered boolean default false, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_material_order_history", pk: "id", sql: "create table if not exists ajw_tool_material_order_history (id text primary key, session_id text, material_id text, nama text, qty numeric default 0, subtotal numeric default 0, ordered boolean default false, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_material_sessions", pk: "id", sql: "create table if not exists ajw_tool_material_sessions (id text primary key, nama text, created_at timestamptz default now(), data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_products", pk: "id", sql: "create table if not exists ajw_tool_products (id text primary key, sku text, judul text, kategori_pertama text, kategori_kedua text, total_stok numeric default 0, penjualan_harian numeric default 0, modal_bobot numeric default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_desc_revision", pk: "id", sql: "create table if not exists ajw_tool_desc_revision (id text primary key, kode_produk text, nama_produk text, status text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_automation_jobs", pk: "id", sql: "create table if not exists ajw_tool_automation_jobs (id text primary key, nama text, channel text, active boolean default true, next_run text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_automation_logs", pk: "id", sql: "create table if not exists ajw_tool_automation_logs (id text primary key, level text, title text, data jsonb not null, created_at timestamptz default now())" },
  { name: "ajw_tool_webhooks", pk: "id", sql: "create table if not exists ajw_tool_webhooks (id text primary key, nama text, method text, url text, active boolean default true, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_blast_rows", pk: "id", sql: "create table if not exists ajw_tool_blast_rows (id text primary key, order_number text, phone text, marketplace text, status text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_blast_history", pk: "id", sql: "create table if not exists ajw_tool_blast_history (id text primary key, order_number text, phone text, marketplace text, status text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_blast_phone_db", pk: "id", sql: "create table if not exists ajw_tool_blast_phone_db (id text primary key, phone text, nama text, marketplace text, total_blast_count int default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_blast_marketing", pk: "id", sql: "create table if not exists ajw_tool_blast_marketing (id text primary key, nama text, schedule_type text, active boolean default false, next_run text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_picking_rows", pk: "id", sql: "create table if not exists ajw_tool_picking_rows (id text primary key, order_number text, marketplace text, sku_gudang text, quantity numeric default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_picking_history", pk: "id", sql: "create table if not exists ajw_tool_picking_history (id text primary key, source_file text, total_rows numeric default 0, status text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_tool_meta", pk: "key", sql: "create table if not exists ajw_tool_meta (key text primary key, value jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_fin_income", pk: "id", sql: "create table if not exists ajw_fin_income (id text primary key, tanggal date, periode_dari date, periode_sampai date, marketplace text, toko text, nominal numeric default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_fin_expense", pk: "id", sql: "create table if not exists ajw_fin_expense (id text primary key, tanggal date, kategori text, nominal numeric default 0, source_type text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_fin_assets", pk: "id", sql: "create table if not exists ajw_fin_assets (id text primary key, tanggal date, type text, nama text, nominal numeric default 0, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_fin_subscriptions", pk: "id", sql: "create table if not exists ajw_fin_subscriptions (id text primary key, nama text, provider text, status text, nominal numeric default 0, next_payment date, billing_cycle text, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_fin_monthly", pk: "key", sql: "create table if not exists ajw_fin_monthly (key text primary key, year int, month int, data jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_fin_meta", pk: "key", sql: "create table if not exists ajw_fin_meta (key text primary key, value jsonb not null, updated_at timestamptz default now())" },
  { name: "ajw_sync_log", pk: "id", sql: "create table if not exists ajw_sync_log (id text primary key, level text, category text, title text, detail jsonb not null, created_at timestamptz default now())" },
  { name: "product_folders", pk: "id", sql: "create table if not exists product_folders (id uuid primary key default gen_random_uuid(), name text not null unique, created_at timestamptz not null default now(), updated_at timestamptz not null default now())" },
  { name: "products", pk: "id", sql: "create table if not exists products (id uuid primary key default gen_random_uuid(), folder_id uuid, title text not null, description text default '', price numeric(14,2) not null default 0, sku text default '', notes text default '', created_at timestamptz not null default now(), updated_at timestamptz not null default now())" },
  { name: "product_media", pk: "id", sql: "create table if not exists product_media (id uuid primary key default gen_random_uuid(), product_id uuid not null, media_type text not null, file_name text, mime_type text, file_size bigint default 0, public_url text, storage_path text, sort_order integer not null default 0, created_at timestamptz not null default now())" },
  { name: "generate_image_workspaces", pk: "id", sql: "create table if not exists generate_image_workspaces (id uuid primary key default gen_random_uuid(), workspace_key text not null unique, workspace_name text not null, description text not null default '', metadata jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now())" },
  { name: "generate_image_brands", pk: "id", sql: "create table if not exists generate_image_brands (id uuid primary key default gen_random_uuid(), workspace_id uuid, brand_name text not null, product_category text not null default '', store_reputation text not null default '', description text not null default '', logo_url text, logo_path text, metadata jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now())" },
  { name: "generate_image_prompts", pk: "id", sql: "create table if not exists generate_image_prompts (id uuid primary key default gen_random_uuid(), workspace_id uuid, feature text not null, prompt_key text not null, title text not null, tag text not null default '', content text not null, usage_count integer not null default 0, is_default boolean not null default false, is_active boolean not null default true, sort_order integer not null default 0, metadata jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now(), unique (workspace_id, feature, prompt_key))" },
  { name: "generate_image_prompt_assets", pk: "id", sql: "create table if not exists generate_image_prompt_assets (id uuid primary key default gen_random_uuid(), prompt_id uuid not null, asset_type text not null default 'thumbnail', asset_url text, asset_path text, file_name text not null default '', sort_order integer not null default 0, metadata jsonb not null default '{}'::jsonb, created_at timestamptz not null default now())" },
  { name: "generation_jobs", pk: "id", sql: "create table if not exists generation_jobs (id uuid primary key default gen_random_uuid(), workspace_id uuid, tool_type text not null, prompt_id uuid, brand_id uuid, request_title text not null default '', provider text not null default '', model text not null default '', status text not null default 'success', prompt_text text not null default '', input_summary text not null default '', output_summary text not null default '', cost_estimate numeric(12,4) not null default 0, input_tokens integer not null default 0, cached_tokens integer not null default 0, output_tokens integer not null default 0, duration_ms integer not null default 0, error_message text not null default '', payload jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now())" },
  { name: "generated_assets", pk: "id", sql: "create table if not exists generated_assets (id uuid primary key default gen_random_uuid(), job_id uuid not null, tool_type text not null, asset_role text not null default 'output', asset_title text not null default '', asset_url text, asset_path text, mime_type text not null default '', width integer, height integer, file_size bigint, sort_order integer not null default 0, metadata jsonb not null default '{}'::jsonb, created_at timestamptz not null default now())" },
  { name: "generate_image_memories", pk: "id", sql: "create table if not exists generate_image_memories (id uuid primary key default gen_random_uuid(), workspace_id uuid, source_job_id uuid, feature text not null, memory_title text not null, memory_text text not null, memory_tags text[] not null default '{}'::text[], rating numeric(5,2) not null default 0, use_count integer not null default 0, metadata jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now())" },
  { name: "generate_image_app_settings", pk: "id", sql: "create table if not exists generate_image_app_settings (id uuid primary key default gen_random_uuid(), workspace_id uuid, setting_group text not null, setting_key text not null, setting_value jsonb not null default '{}'::jsonb, created_at timestamptz not null default now(), updated_at timestamptz not null default now(), unique (workspace_id, setting_group, setting_key))" },
  { name: "shopee_chat_tokens", pk: "shop_id", sql: "create table if not exists shopee_chat_tokens (shop_id text primary key, access_token text not null, refresh_token text, expire_in bigint default 0, updated_at timestamptz default now())" },
  { name: "shopee_chat_conversations", pk: "conversation_id", sql: "create table if not exists shopee_chat_conversations (conversation_id text primary key, shop_id text not null, to_id text, to_name text, to_avatar text, unread_count integer default 0, pinned integer default 0, latest_message_id text, latest_message_type text, latest_message_text text, latest_from_id text, has_unreplied integer default 0, last_message_timestamp bigint default 0, raw_json text, updated_at timestamptz default now())" },
  { name: "shopee_chat_messages", pk: "message_id", sql: "create table if not exists shopee_chat_messages (message_id text primary key, conversation_id text not null, shop_id text not null, message_type text, from_id text, to_id text, created_timestamp bigint default 0, content_text text, content_order_sn text, raw_json text, updated_at timestamptz default now())" },
  { name: "shopee_chat_webhook_events", pk: "id", sql: "create table if not exists shopee_chat_webhook_events (id text primary key, event_key text, payload text, created_at timestamptz default now())" },
  { name: "shopee_chat_quick_replies", pk: "id", sql: "create table if not exists shopee_chat_quick_replies (id text primary key, shop_id text not null, title text, content text not null, group_name text default 'Umum', position integer default 0, updated_at timestamptz default now())" },
  { name: "shopee_chat_orders", pk: "id", sql: "create table if not exists shopee_chat_orders (id text primary key, shop_id text not null, conversation_id text, order_sn text not null, customer_name text, order_status text, create_time bigint default 0, pay_time bigint default 0, total_amount numeric default 0, item_count integer default 0, items_json text, raw_json text, updated_at timestamptz default now())" },
  { name: "shopee_chat_products", pk: "id", sql: "create table if not exists shopee_chat_products (id text primary key, shop_id text not null, item_id text not null, item_name text, sku text, price_info text, stock integer default 0, image_url text, status text, raw_json text, updated_at timestamptz default now())" },
  { name: "shopee_chat_ai_knowledge", pk: "id", sql: "create table if not exists shopee_chat_ai_knowledge (id text primary key, shop_id text not null, keyword text not null, template text not null, group_name text default 'General', priority integer default 0, active integer default 1, position integer default 0, source text default 'manual', updated_at timestamptz default now())" },
  { name: "shopee_chat_ai_settings", pk: "shop_id", sql: "create table if not exists shopee_chat_ai_settings (shop_id text primary key, ai_enabled integer default 0, require_approval integer default 1, provider text default 'smart', model text default '', prompt_preset text default '', updated_at timestamptz default now())" },
  { name: "shopee_chat_ai_drafts", pk: "id", sql: "create table if not exists shopee_chat_ai_drafts (id text primary key, shop_id text not null, conversation_id text not null, source_message_id text default '', source_text text default '', draft_text text not null, provider text default 'smart', model text default '', knowledge_refs text default '[]', status text default 'draft', created_at timestamptz default now(), updated_at timestamptz default now())" },
  { name: "shopee_chat_ai_learning", pk: "id", sql: "create table if not exists shopee_chat_ai_learning (id text primary key, shop_id text not null, conversation_id text default '', customer_text text not null, seller_text text not null, source text default 'manual_reply', score numeric, notes text default '', metadata_json text default '', updated_at timestamptz default now())" },
  { name: "shopee_chat_ai_secrets", pk: "id", sql: "create table if not exists shopee_chat_ai_secrets (id text primary key, shop_id text not null, provider text not null, encrypted_key text not null, key_hint text default '', updated_at timestamptz default now())" }
];

export const AJW_NEON_POST_SCHEMA_SQL = `
create index if not exists idx_ajw_fin_income_tanggal on ajw_fin_income(tanggal);
create index if not exists idx_ajw_fin_expense_tanggal on ajw_fin_expense(tanggal);
create index if not exists idx_ajw_customer_data_order_number on ajw_customer_data(order_number);
create index if not exists idx_ajw_customer_data_phone on ajw_customer_data(phone);
create index if not exists idx_ajw_tool_products_sku on ajw_tool_products(sku);
create index if not exists idx_ajw_tool_picking_rows_order on ajw_tool_picking_rows(order_number);
create index if not exists idx_shopee_chat_conversations_shop_ts on shopee_chat_conversations(shop_id, last_message_timestamp desc);
create index if not exists idx_shopee_chat_messages_conv_ts on shopee_chat_messages(conversation_id, created_timestamp desc);
create unique index if not exists idx_shopee_chat_ai_secrets_shop_provider on shopee_chat_ai_secrets(shop_id, provider);
create or replace view generate_image_prompt_library_view as
select p.*, coalesce(jsonb_agg(jsonb_build_object('id', a.id, 'asset_type', a.asset_type, 'asset_url', a.asset_url, 'asset_path', a.asset_path, 'file_name', a.file_name, 'sort_order', a.sort_order, 'metadata', a.metadata) order by a.sort_order, a.created_at) filter (where a.id is not null), '[]'::jsonb) as assets
from generate_image_prompts p
left join generate_image_prompt_assets a on a.prompt_id = p.id
group by p.id;
`;
